<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Todo List</title>

    <link
      rel="stylesheet"
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
    <link rel="preconnect" href="https://fonts.googleapis.com"><link rel="preconnect" href="https://fonts.gstatic.com" crossorigin><link href="https://fonts.googleapis.com/css2?family=Rubik:ital,wght@0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Unbounded:wght@200;300;400;500;600;700;800;900&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css" />
  </head>
  <body>
    <!-- <h1>Your Todo List:</h1> -->
    <article class="carousel">
    <% for(let j=0;j<tags.length;j++){ %>
        <section class="tags">
            <h2><%= tags[j]?tags[j]:"no-tag" %></h2>
            <ul class="todoItems">
                <% for(let i=0;i<items.length;i++){%>
                    <% if(items[i].tag == tags[j]){ %> 
                        <li class="item">
                            <% if(items[i].completed===true) {%>
                                <span class="completed task"><%= items[i].item %></span>
                            <% }else{ %>  
                                <span class="task"><%= items[i].item %></span>
                            <% } %> 
                            <span class="fa fa-trash"></span>
                        </li>
                    <% } %> 
                <% } %> 
            </ul>
        </section>
    <% } %>  
    </article>

    <!-- <ul class="todoItems">
        <% for(let i=0;i<items.length;i++){%>
            <li class="item">
                <% if(items[i].completed===true) {%>
                    <span class="completed task"><%= items[i].item %></span>
                <% }else{ %>  
                    <span class="task"><%= items[i].item %></span>
                <% } %> 
                <span class="fa fa-trash"></span>
            </li>
        <% } %> 
    </ul> -->
    <!-- <h2>Left to do: <%= left %></h2> -->
    <footer>
        <h3>Add a Todo:</h3>
        <form action="/addTodo" method="post">
            <datalist id="tags-list">
                <% for(let j=0;j<tags.length;j++){ %>
                    <option value=<%= tags[j] %>></option>
                <% } %> 
            </datalist>
            <input type="text" placeholder="Task" name="todoItem">
            <input type="text" placeholder="Tag" name="tagItem" list="tags-list">
            <input type="submit">
        </form>
    </footer>
    <div class="background">
        <h2 class="background-text">TODO: <%= left %>  </h2>
        <h2 class="background-text">COMPLETED: <%= left %>  </h2>
    </div>

    <script src="js/main.js"></script>
  </body>
</html>
